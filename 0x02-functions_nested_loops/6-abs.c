#include "main.h"

/**
 * _abs - compute the value of an integer
 * @t: the integer to be computted
 * Return: the absolute value
 */
int _abs(int t)
{
	if (t >= 0)
		return (t);
	else
		return (-t);
}
